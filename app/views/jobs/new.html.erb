<div data-controller="ajax-append" class="space-y-6 sm:px-6 md:px-0 md:col-span-9">
  <%= form_with model: @job do |form| %>
  <div class="shadow sm:rounded-md sm:overflow-hidden">
    <div class="bg-white py-6 px-4 space-y-6 sm:p-6">
      <div>
        <h3 class="text-lg leading-6 font-medium text-gray-900">Jobs</h3>
        <p class="mt-1 text-sm text-gray-500">
          Enqueue background jobs with a configurable processing time to test capacity issues.
        </p>
      </div>

      <fieldset>
        <%= render "shared/latency_field", form: form do %>
        <p class="text-sm text-gray-500">How long do you want the job to work?</p>
        <% end %>
      </fieldset>

      <fieldset>
        <%= render "shared/strategy_field", form: form do %>
        <p class="text-sm text-gray-500">
          Sleeping and I/O (such waiting on a database query) allow other threads on the same
          process to run, while CPU tasks consume the entire process.
        </p>
        <% end %>
      </fieldset>

      <fieldset><%= render "shared/queue_field", form: form %></fieldset>
    </div>
    <div class="px-4 py-3 bg-gray-50 sm:px-6">
      <button
        type="submit"
        class="
          bg-indigo-600
          border border-transparent
          rounded-md
          shadow-sm
          py-2
          px-4
          inline-flex
          justify-center
          text-sm
          font-medium
          text-white
          hover:bg-indigo-700
          focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-600
        "
      >
        Enqueue job
      </button>
    </div>
  </div>
  <% end %>

  <div class="flex flex-col">
    <div class="-my-2 overflow-x-auto sm:-mx-6 lg:-mx-8">
      <div class="py-2 align-middle inline-block min-w-full sm:px-6 lg:px-8">
        <div class="shadow overflow-hidden border-b border-gray-200 sm:rounded-lg">
          <table class="min-w-full divide-y divide-gray-200">
            <thead class="bg-gray-50">
              <tr>
                <th
                  scope="col"
                  class="
                    px-6
                    py-3
                    text-left text-xs
                    font-medium
                    text-gray-500
                    uppercase
                    tracking-wider
                  "
                >
                  Enqueued at
                </th>
                <th
                  scope="col"
                  class="
                    px-6
                    py-3
                    text-left text-xs
                    font-medium
                    text-gray-500
                    uppercase
                    tracking-wider
                  "
                >
                  Queue
                </th>
                <th
                  scope="col"
                  class="
                    px-6
                    py-3
                    text-left text-xs
                    font-medium
                    text-gray-500
                    uppercase
                    tracking-wider
                  "
                >
                  Latency
                </th>
              </tr>
            </thead>
            <tbody
              data-ajax-append-target="container"
              class="bg-white divide-y divide-gray-200"
            ></tbody>
          </table>
        </div>
      </div>
    </div>
  </div>
</div>
